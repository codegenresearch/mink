# Configuration for the Sphinx documentation builder.">"># This file configures the Sphinx documentation build process for the Mink project.">">import pathlib">">import toml">">">"># Load version from pyproject.toml">">project_root: pathlib.Path = pathlib.Path(__file__).resolve().parent.parent">">pyproject_path: pathlib.Path = project_root / "pyproject.toml">">pyproject = toml.load(pyproject_path)"># Ensure the version format is handled appropriately">">version: str = pyproject["tool"]["poetry"]["version"]">">">"># Project information">">project: str = "mink""># Copyright notice">">copyright: str = "2024, Kevin Zakka""># Author of the project">">author: str = "Kevin Zakka"">">">"># General configuration">">extensions: list[str] = [">">    "sphinx.ext.autodoc","># Include documentation from docstrings">">    "sphinx.ext.coverage","># Coverage checker">">    "sphinx-mathjax-offline","># MathJax for rendering math equations">">    "sphinx.ext.napoleon","># Support for Google style docstrings">">    "sphinx_favicon","># Favicon management">">]">">">"># Type hints for autodoc">">autodoc_typehints: str = "both""># Separate class signature and docstring">">autodoc_class_signature: str = "separated""># Default options for autodoc">">autodoc_default_options: dict[str, bool | str | list[str]] = {">">    "members": True,"># Include all members">">    "member-order": "bysource","># Order members by source>"># Do not include inherited members">">    "inherited-members": False,"># Exclude certain special methods">">    "exclude-members": ["__init__", "__post_init__", "__new__"],">">}">">">"># Paths for templates">">templates_path: list[str] = [">    "_templates","># Directory containing custom templates">">]">">">"># Patterns to exclude from the build">">exclude_patterns: list[str] = [">    "_build","># Build directory">">    "Thumbs.db","># Thumbnail database">">    ".DS_Store","># macOS metadata file">">]">">">"># Source file suffixes">">source_suffix: dict[str, str] = {">    ".rst": "restructuredtext","># ReStructuredText files">">}">">">"># Pygments syntax highlighting style">">pygments_style: str = "sphinx""># Use Sphinx default style">">">"># Type aliases for autodoc">">autodoc_type_aliases: dict[str, str] = {">    "npt.ArrayLike": "ArrayLike","># Alias for ArrayLike type">">}">">">"># HTML output options">">html_theme: str = "sphinx_rtd_theme""># Use Read the Docs theme">">">"># Base name for HTML help output">">htmlhelp_basename: str = "minkdoc""># Base name for the generated help file">">