import abc\nfrom typing import Union, overload\nimport numpy as np\nfrom typing_extensions import Self\n\nclass MatrixLieGroup(abc.ABC):\n    \"\"\"Interface definition for matrix Lie groups.\n\n    This class defines the abstract base class for matrix Lie groups, which are\n    mathematical structures combining group properties with matrix representations.\n    Each subclass should implement the abstract methods for specific functionality.\n\n    Attributes:\n        matrix_dim (int): Dimension of the square matrix representation.\n        parameters_dim (int): Dimension of the underlying parameterization.\n        tangent_dim (int): Dimension of the tangent space.\n        space_dim (int): Dimension of the coordinate space that can be transformed.\n    \"\"\"\n\n    matrix_dim: int\n    parameters_dim: int\n    tangent_dim: int\n    space_dim: int\n\n    @overload\n    def __matmul__(self, other: Self) -> Self: ...\n\n    @overload\n    def __matmul__(self, other: np.ndarray) -> np.ndarray: ...\n\n    def __matmul__(self, other: Union[Self, np.ndarray]) -> Union[Self, np.ndarray]:\n        \"\"\"Overload of the @ operator for matrix multiplication and transformation.\n\n        Args:\n            other (Union[Self, np.ndarray]): The operand to multiply with.\n\n        Returns:\n            Union[Self, np.ndarray]: The result of the operation.\n        \"\"\"\n        if isinstance(other, np.ndarray):\n            return self.apply(target=other)\n        assert isinstance(other, MatrixLieGroup)\n        return self.multiply(other=other)\n\n    @classmethod\n    @abc.abstractmethod\n    def identity(cls) -> Self:\n        \"\"\"Returns the identity element of the group.\n\n        Returns:\n            Self: The identity element.\n        \"\"\"\n        raise NotImplementedError\n\n    @classmethod\n    @abc.abstractmethod\n    def from_matrix(cls, matrix: np.ndarray) -> Self:\n        \"\"\"Creates a group element from a matrix.\n\n        Args:\n            matrix (np.ndarray): The matrix representation.\n\n        Returns:\n            Self: The group element.\n        \"\"\"\n        raise NotImplementedError\n\n    @classmethod\n    @abc.abstractmethod\n    def sample_uniform(cls) -> Self:\n        \"\"\"Draws a uniform sample from the group.\n\n        Returns:\n            Self: A randomly sampled group element.\n        \"\"\"\n        raise NotImplementedError\n\n    @abc.abstractmethod\n    def as_matrix(self) -> np.ndarray:\n        \"\"\"Returns the matrix representation.\n\n        Returns:\n            np.ndarray: The matrix representation.\n        \"\"\"\n        raise NotImplementedError\n\n    @abc.abstractmethod\n    def parameters(self) -> np.ndarray:\n        \"\"\"Returns the underlying parameterization.\n\n        Returns:\n            np.ndarray: The parameters.\n        \"\"\"\n        raise NotImplementedError\n\n    @abc.abstractmethod\n    def apply(self, target: np.ndarray) -> np.ndarray:\n        \"\"\"Applies the group action to a target point or set of points.\n\n        Args:\n            target (np.ndarray): The point or points to transform.\n\n        Returns:\n            np.ndarray: The transformed point or points.\n        \"\"\"\n        raise NotImplementedError\n\n    @abc.abstractmethod\n    def multiply(self, other: Self) -> Self:\n        \"\"\"Composes this transformation with another.\n\n        Args:\n            other (Self): The other transformation.\n\n        Returns:\n            Self: The composed transformation.\n        \"\"\"\n        raise NotImplementedError\n\n    @classmethod\n    @abc.abstractmethod\n    def exp(cls, tangent: np.ndarray) -> Self:\n        \"\"\"Computes the exponential map of a tangent vector.\n\n        Args:\n            tangent (np.ndarray): The tangent vector.\n\n        Returns:\n            Self: The group element.\n        \"\"\"\n        raise NotImplementedError\n\n    @abc.abstractmethod\n    def log(self) -> np.ndarray:\n        \"\"\"Computes the logarithm map of the group element.\n\n        Returns:\n            np.ndarray: The tangent vector.\n        \"\"\"\n        raise NotImplementedError\n\n    @abc.abstractmethod\n    def adjoint(self) -> np.ndarray:\n        \"\"\"Computes the adjoint representation.\n\n        Returns:\n            np.ndarray: The adjoint matrix.\n        \"\"\"\n        raise NotImplementedError\n\n    @abc.abstractmethod\n    def inverse(self) -> Self:\n        \"\"\"Computes the inverse of the group element.\n\n        Returns:\n            Self: The inverse.\n        \"\"\"\n        raise NotImplementedError\n\n    @abc.abstractmethod\n    def normalize(self) -> Self:\n        \"\"\"Normalizes the group element.\n\n        Returns:\n            Self: The normalized element.\n        \"\"\"\n        raise NotImplementedError\n\n    def rplus(self, other: np.ndarray) -> Self:\n        \"\"\"Applies the right plus operation.\n\n        Args:\n            other (np.ndarray): The tangent vector.\n\n        Returns:\n            Self: The resulting group element.\n        \"\"\"\n        return self @ self.exp(other)\n\n    def rminus(self, other: Self) -> np.ndarray:\n        \"\"\"Applies the right minus operation.\n\n        Args:\n            other (Self): The other group element.\n\n        Returns:\n            np.ndarray: The resulting tangent vector.\n        \"\"\"\n        return (other.inverse() @ self).log()\n\n    def lplus(self, other: np.ndarray) -> Self:\n        \"\"\"Applies the left plus operation.\n\n        Args:\n            other (np.ndarray): The tangent vector.\n\n        Returns:\n            Self: The resulting group element.\n        \"\"\"\n        return self.exp(other) @ self\n\n    def lminus(self, other: Self) -> np.ndarray:\n        \"\"\"Applies the left minus operation.\n\n        Args:\n            other (Self): The other group element.\n\n        Returns:\n            np.ndarray: The resulting tangent vector.\n        \"\"\"\n        return (self @ other.inverse()).log()\n\n    def plus(self, other: np.ndarray) -> Self:\n        \"\"\"Alias for rplus.\n\n        Args:\n            other (np.ndarray): The tangent vector.\n\n        Returns:\n            Self: The resulting group element.\n        \"\"\"\n        return self.rplus(other)\n\n    def minus(self, other: Self) -> np.ndarray:\n        \"\"\"Alias for rminus.\n\n        Args:\n            other (Self): The other group element.\n\n        Returns:\n            np.ndarray: The resulting tangent vector.\n        \"\"\"\n        return self.rminus(other)\n\n    @classmethod\n    @abc.abstractmethod\n    def ljac(cls, other: np.ndarray) -> np.ndarray:\n        \"\"\"Computes the left Jacobian.\n\n        Args:\n            other (np.ndarray): The tangent vector.\n\n        Returns:\n            np.ndarray: The left Jacobian matrix.\n        \"\"\"\n        raise NotImplementedError\n\n    @classmethod\n    @abc.abstractmethod\n    def ljacinv(cls, other: np.ndarray) -> np.ndarray:\n        \"\"\"Computes the inverse of the left Jacobian.\n\n        Args:\n            other (np.ndarray): The tangent vector.\n\n        Returns:\n            np.ndarray: The inverse of the left Jacobian matrix.\n        \"\"\"\n        raise NotImplementedError\n\n    @classmethod\n    def rjac(cls, other: np.ndarray) -> np.ndarray:\n        \"\"\"Computes the right Jacobian.\n\n        Args:\n            other (np.ndarray): The tangent vector.\n\n        Returns:\n            np.ndarray: The right Jacobian matrix.\n        \"\"\"\n        return cls.ljac(-other)\n\n    @classmethod\n    def rjacinv(cls, other: np.ndarray) -> np.ndarray:\n        \"\"\"Computes the inverse of the right Jacobian.\n\n        Args:\n            other (np.ndarray): The tangent vector.\n\n        Returns:\n            np.ndarray: The inverse of the right Jacobian matrix.\n        \"\"\"\n        return cls.ljacinv(-other)\n\n    def jlog(self) -> np.ndarray:\n        \"\"\"Computes the Jacobian of the logarithm map.\n\n        Returns:\n            np.ndarray: The Jacobian matrix.\n        \"\"\"\n        return self.rjacinv(self.log())\n