from typing import Optional\n\nimport mujoco\nimport numpy as np\n\nfrom . import constants as consts\nfrom . import exceptions\nfrom .lie import SE3, SO3\n\n\nclass RobotModelConfiguration:\n    """Encapsulates a MuJoCo model and its associated data for convenient access to kinematic quantities.\n\n    This class provides methods to access and update the kinematic quantities\n    of a robot model, such as frame transforms and Jacobians. It ensures that forward\n    kinematics is computed at each time step, allowing the user to query up-to-date\n    information about the robot's state.\n\n    Supported frames include `body`, `geom`, and `site`.\n    """\n\n    def __init__(\n        self,\n        model: mujoco.MjModel,\n        initial_configuration: Optional[np.ndarray] = None,\n    ) -> None:\n        """Initialize the RobotModelConfiguration object.\n\n        Args:\n            model (mujoco.MjModel): MuJoCo model.\n            initial_configuration (Optional[np.ndarray]): Initial configuration vector. If None,\n                the configuration is initialized to the reference configuration `qpos0`.\n        """\n        self.model = model\n        self.data = mujoco.MjData(model)\n        self.update(initial_configuration)\n\n    def update(self, configuration: Optional[np.ndarray] = None) -> None:\n        """Run forward kinematics to update the state.\n\n        Args:\n            configuration (Optional[np.ndarray]): Configuration vector to override internal\n                data.qpos with. If None, the current configuration is used.\n        """\n        if configuration is not None:\n            self.data.qpos = configuration\n        mujoco.mj_kinematics(self.model, self.data)\n        mujoco.mj_comPos(self.model, self.data)\n\n    def update_from_keyframe(self, key_name: str) -> None:\n        """Update the configuration from a keyframe.\n\n        Args:\n            key_name (str): Name of the keyframe.\n\n        Raises:\n            InvalidKeyframe: If no keyframe with the specified name is found.\n        """\n        key_id = mujoco.mj_name2id(self.model, mujoco.mjtObj.mjOBJ_KEY, key_name)\n        if key_id == -1:\n            raise exceptions.InvalidKeyframe(key_name, self.model)\n        self.update(self.model.key_qpos[key_id])\n\n    def check_limits(self, tolerance: float = 1e-6, raise_exception: bool = True) -> None:\n        """Check that the current configuration is within joint limits.\n\n        Args:\n            tolerance (float): Tolerance in radians.\n            raise_exception (bool): If True, raise an exception if the configuration is\n                outside the limits. If False, print a warning.\n\n        Raises:\n            NotWithinConfigurationLimits: If the configuration is outside the limits and\n                raise_exception is True.\n        """\n        for joint_index in range(self.model.njnt):\n            joint_type = self.model.jnt_type[joint_index]\n            if (\n                joint_type == mujoco.mjtJoint.mjJNT_FREE\n                or not self.model.jnt_limited[joint_index]\n            ):\n                continue\n            position_address = self.model.jnt_qposadr[joint_index]\n            joint_value = self.q[position_address]\n            joint_min = self.model.jnt_range[joint_index, 0]\n            joint_max = self.model.jnt_range[joint_index, 1]\n            if joint_value < joint_min - tolerance or joint_value > joint_max + tolerance:\n                if raise_exception:\n                    raise exceptions.NotWithinConfigurationLimits(\n                        joint_id=joint_index,\n                        value=joint_value,\n                        lower=joint_min,\n                        upper=joint_max,\n                        model=self.model,\n                    )\n                else:\n                    print(\n                        f"Value {joint_value} at index {joint_index} is out of limits: "\n                        f"[{joint_min}, {joint_max}]"\n                    )\n\n    def get_frame_jacobian(self, frame_name: str, frame_type: str) -> np.ndarray:\n        """Compute the Jacobian matrix of a frame velocity.\n\n        Denoting our frame by \( B \) and the world frame by \( W \), the\n        Jacobian matrix \( {}_B J_{WB} \) is related to the body velocity\n        \( {}_B v_{WB} \) by:\n\n        .. math::\n\n            {}_B v_{WB} = {}_B J_{WB} \dot{q}\n\n        Args:\n            frame_name (str): Name of the frame in the MJCF.\n            frame_type (str): Type of frame. Can be `geom`, `body`, or `site`.\n\n        Returns:\n            np.ndarray: Jacobian \( {}_B J_{WB} \) of the frame.\n\n        Raises:\n            UnsupportedFrame: If the frame type is not supported.\n            InvalidFrame: If the frame name is not found.\n        """\n        if frame_type not in consts.SUPPORTED_FRAMES:\n            raise exceptions.UnsupportedFrame(frame_type, consts.SUPPORTED_FRAMES)\n        frame_id = mujoco.mj_name2id(\n            self.model, consts.FRAME_TO_ENUM[frame_type], frame_name\n        )\n        if frame_id == -1:\n            raise exceptions.InvalidFrame(\n                frame_name=frame_name,\n                frame_type=frame_type,\n                model=self.model,\n            )\n        jacobian = np.empty((6, self.model.nv))\n        jacobian_function = consts.FRAME_TO_JAC_FUNC[frame_type]\n        jacobian_function(self.model, self.data, jacobian[:3], jacobian[3:], frame_id)\n        xmat = getattr(self.data, consts.FRAME_TO_XMAT_ATTR[frame_type])[frame_id]\n        R_wf = SO3.from_matrix(xmat.reshape(3, 3))\n        A_fw = SE3.from_rotation(R_wf.inverse()).adjoint()\n        jacobian = A_fw @ jacobian\n        return jacobian\n\n    def get_transform_frame_to_world(self, frame_name: str, frame_type: str) -> SE3:\n        """Get the pose of a frame at the current configuration.\n\n        Args:\n            frame_name (str): Name of the frame in the MJCF.\n            frame_type (str): Type of frame. Can be `geom`, `body`, or `site`.\n\n        Returns:\n            SE3: The pose of the frame in the world frame.\n\n        Raises:\n            UnsupportedFrame: If the frame type is not supported.\n            InvalidFrame: If the frame name is not found.\n        """\n        if frame_type not in consts.SUPPORTED_FRAMES:\n            raise exceptions.UnsupportedFrame(frame_type, consts.SUPPORTED_FRAMES)\n        frame_id = mujoco.mj_name2id(\n            self.model, consts.FRAME_TO_ENUM[frame_type], frame_name\n        )\n        if frame_id == -1:\n            raise exceptions.InvalidFrame(\n                frame_name=frame_name,\n                frame_type=frame_type,\n                model=self.model,\n            )\n        position = getattr(self.data, consts.FRAME_TO_POS_ATTR[frame_type])[frame_id]\n        orientation_matrix = getattr(self.data, consts.FRAME_TO_XMAT_ATTR[frame_type])[frame_id]\n        return SE3.from_rotation_and_translation(\n            rotation=SO3.from_matrix(orientation_matrix.reshape(3, 3)),\n            translation=position,\n        )\n\n    def integrate(self, velocity: np.ndarray, time_step: float) -> np.ndarray:\n        """Integrate a velocity starting from the current configuration.\n\n        Args:\n            velocity (np.ndarray): Velocity vector in tangent space.\n            time_step (float): Integration duration in seconds.\n\n        Returns:\n            np.ndarray: New configuration after integration.\n        """\n        configuration = self.data.qpos.copy()\n        mujoco.mj_integratePos(self.model, configuration, velocity, time_step)\n        return configuration\n\n    def integrate_inplace(self, velocity: np.ndarray, time_step: float) -> None:\n        """Integrate a velocity and update the current configuration in place.\n\n        Args:\n            velocity (np.ndarray): Velocity vector in tangent space.\n            time_step (float): Integration duration in seconds.\n        """\n        mujoco.mj_integratePos(self.model, self.data.qpos, velocity, time_step)\n        self.update()\n\n    # Aliases.\n\n    @property\n    def q(self) -> np.ndarray:\n        """np.ndarray: The current configuration vector."""\n        return self.data.qpos.copy()\n\n    @property\n    def nv(self) -> int:\n        """int: The dimension of the tangent space."""\n        return self.model.nv\n\n    @property\n    def nq(self) -> int:\n        """int: The dimension of the configuration space."""\n        return self.model.nq\n