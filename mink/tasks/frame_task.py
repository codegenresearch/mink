from __future__ import annotations\nfrom typing import Optional\nimport numpy as np\nimport numpy.typing as npt\nfrom ..configuration import Configuration\nfrom ..lie import SE3\nfrom .exceptions import TargetNotSet, TaskDefinitionError\nfrom .task import Task\nclass FrameTask(Task):\n    """Regulate the pose of a robot frame in the world frame.\\n    This task aims to align a specified frame of the robot with a target pose in the world frame. The frame can be specified by its name and type (body, geom, or site). The task defines both position and orientation costs, which are used to penalize deviations from the target pose.\\n    Attributes:\\n        frame_name (str): Name of the frame to regulate.\\n        frame_type (str): The type of the frame: `body`, `geom`, or `site`.\\n        transform_target_to_world (Optional[SE3]): Target pose of the frame in the world frame.\\