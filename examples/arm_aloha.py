from pathlib import Path\n\nimport mujoco\nimport mujoco.viewer\nimport numpy as np\nfrom loop_rate_limiters import RateLimiter\n\nimport mink\n\n# Define the path to the XML file containing the model description.\n_HERE = Path(__file__).parent\n_XML_PATH = _HERE / "aloha" / "scene.xml"\n\n# Joint names for a single arm.\nJOINT_NAMES = [\n    "waist",\n    "shoulder",\n    "elbow",\n    "forearm_roll",\n    "wrist_angle",\n    "wrist_rotate",\n]\n\n# Velocity limits for each joint, sourced from:\n# https://github.com/Interbotix/interbotix_ros_manipulators/blob/main/interbotix_ros_xsarms/interbotix_xsarm_descriptions/urdf/vx300s.urdf.xacro\nVELOCITY_LIMITS = {joint: np.pi for joint in JOINT_NAMES}\n\n\nif __name__ == "__main__":\n    # Load the model and data from the XML file.\n    model: mujoco.MjModel = mujoco.MjModel.from_xml_path(_XML_PATH.as_posix())\n    data: mujoco.MjData = mujoco.MjData(model)\n\n    # Collect joint and actuator IDs for both arms.\n    joint_ids: list[str] = []\n    joint_velocity_limits: dict[str, float] = {}\n    for arm_prefix in ["left", "right"]:\n        for joint_name in JOINT_NAMES:\n            full_joint_name = f"{arm_prefix}/{joint_name}"\n            joint_ids.append(full_joint_name)\n            joint_velocity_limits[full_joint_name] = VELOCITY_LIMITS[joint_name]\n    degree_of_freedom_ids: np.ndarray = np.array([model.joint(name).id for name in joint_ids])\n    actuator_ids: np.ndarray = np.array([model.actuator(name).id for name in joint_ids])\n\n    # Initialize the configuration for the model.\n    configuration: mink.Configuration = mink.Configuration(model)\n\n    # Define tasks for both end effectors.\n    left_end_effector_task: mink.FrameTask = mink.FrameTask(\n        frame_name="left/gripper",\n        frame_type="site",\n        position_cost=1.0,\n        orientation_cost=1.0,\n        lm_damping=1.0,\n    )\n    right_end_effector_task: mink.FrameTask = mink.FrameTask(\n        frame_name="right/gripper",\n        frame_type="site",\n        position_cost=1.0,\n        orientation_cost=1.0,\n        lm_damping=1.0,\n    )\n    tasks: list[mink.FrameTask] = [left_end_effector_task, right_end_effector_task]\n\n    # Set up collision avoidance between wrists and the table, and between the two wrists.\n    left_wrist_geoms: list[int] = mink.get_subtree_geom_ids(model, model.body("left/wrist_link").id)\n    right_wrist_geoms: list[int] = mink.get_subtree_geom_ids(model, model.body("right/wrist_link").id)\n    frame_geoms: list[int] = mink.get_body_geom_ids(model, model.body("metal_frame").id)\n    collision_pairs: list[tuple[list[int], list[int]]] = [\n        (left_wrist_geoms, right_wrist_geoms),\n        (left_wrist_geoms + right_wrist_geoms, frame_geoms + ["table"]),\n    ]\n    collision_avoidance_limit: mink.CollisionAvoidanceLimit = mink.CollisionAvoidanceLimit(\n        model=model,\n        geom_pairs=collision_pairs,\n        minimum_distance_from_collisions=0.05,\n        collision_detection_distance=0.1,\n    )\n\n    # Define configuration limits.\n    configuration_limits: list[mink.Limit] = [\n        mink.ConfigurationLimit(model=model),\n        mink.VelocityLimit(model, joint_velocity_limits),\n        collision_avoidance_limit,\n    ]\n\n    # Mocap target IDs for both arms.\n    left_target_mocap_id: int = model.body("left/target").mocapid[0]\n    right_target_mocap_id: int = model.body("right/target").mocapid[0]\n\n    # IK solver settings.\n    solver: str = "quadprog"\n    position_threshold: float = 1e-4\n    orientation_threshold: float = 1e-4\n    max_iterations: int = 20\n\n    with mujoco.viewer.launch_passive(\n        model=model, data=data, show_left_ui=False, show_right_ui=False\n    ) as viewer:\n        mujoco.mjv_defaultFreeCamera(model, viewer.cam)\n\n        # Initialize to the neutral pose keyframe.\n        mujoco.mj_resetDataKeyframe(model, data, model.key("neutral_pose").id)\n        configuration.update(data.qpos)\n        mujoco.mj_forward(model, data)\n\n        # Initialize mocap targets at the end-effector sites.\n        mink.move_mocap_to_frame(model, data, "left/target", "left/gripper", "site")\n        mink.move_mocap_to_frame(model, data, "right/target", "right/gripper", "site")\n\n        rate_limiter: RateLimiter = RateLimiter(frequency=200.0)\n        while viewer.is_running():\n            # Update task targets.\n            left_end_effector_task.set_target(mink.SE3.from_mocap_name(model, data, "left/target"))\n            right_end_effector_task.set_target(mink.SE3.from_mocap_name(model, data, "right/target"))\n\n            # Compute velocity and integrate into the next configuration.\n            for _ in range(max_iterations):\n                velocity: np.ndarray = mink.solve_ik(\n                    configuration,\n                    tasks,\n                    rate_limiter.dt,\n                    solver,\n                    limits=configuration_limits,\n                    damping=1e-3,\n                )\n                configuration.integrate_inplace(velocity, rate_limiter.dt)\n\n                left_error: np.ndarray = left_end_effector_task.compute_error(configuration)\n                left_position_achieved: bool = np.linalg.norm(left_error[:3]) <= position_threshold\n                left_orientation_achieved: bool = np.linalg.norm(left_error[3:]) <= orientation_threshold\n\n                right_error: np.ndarray = right_end_effector_task.compute_error(configuration)\n                right_position_achieved: bool = np.linalg.norm(right_error[:3]) <= position_threshold\n                right_orientation_achieved: bool = np.linalg.norm(right_error[3:]) <= orientation_threshold\n\n                if (\n                    left_position_achieved\n                    and left_orientation_achieved\n                    and right_position_achieved\n                    and right_orientation_achieved\n                ):\n                    break\n\n            data.ctrl[actuator_ids] = configuration.q[degree_of_freedom_ids]\n            mujoco.mj_step(model, data)\n\n            # Visualize at fixed FPS.\n            viewer.sync()\n            rate_limiter.sleep()\n