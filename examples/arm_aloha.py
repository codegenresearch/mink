from pathlib import Path, Sequence, Optional, List, Dict, str, np, mujoco, mujoco.viewer, RateLimiter, mink\nfrom typing import Optional, Sequence, List, Dict, str, np, mujoco, mujoco.viewer, RateLimiter, mink\nimport mujoco\nimport mujoco.viewer\nimport numpy as np\nfrom loop_rate_limiters import RateLimiter\nimport mink\n# Path to the XML file containing the model definition.\n_HERE = Path(__file__).parent\n_XML = _HERE / "aloha" / "scene.xml"\n# Joint names for a single arm.\n_JOINT_NAMES = ["waist", "shoulder", "elbow", "forearm_roll", "wrist_angle", "wrist_rotate"]\n# Velocity limits for each joint, taken from the URDF specifications.\n_VELOCITY_LIMITS = {k: np.pi for k in _JOINT_NAMES}\ndef compensate_gravity(model: mujoco.MjModel, data: mujoco.MjData, subtree_ids: Sequence[int], qfrc_applied: Optional[np.ndarray] = None) -> None:\n    """Compute forces to counteract gravity for the given subtrees.\\n    Args:\\n        model (mujoco.MjModel): Mujoco model.\\n        data (mujoco.MjData): Mujoco data.\\n        subtree_ids (Sequence[int]): List of subtree ids. A subtree is defined as the kinematic tree\\n            starting at the body and including all its descendants. Gravity\\n            compensation forces will be applied to all bodies in the subtree.\\n        qfrc_applied (Optional[np.ndarray]): Optional array to store the computed forces. If not provided,\\n            the applied forces in `data` are used.\\