from pathlib import Path\n\nimport mujoco\nimport mujoco.viewer\nimport numpy as np\nfrom loop_rate_limiters import RateLimiter\n\nimport mink\n\n# Define the path to the XML file containing the model\n_HERE = Path(__file__).parent\n_XML_PATH = _HERE / "universal_robots_ur5e" / "scene.xml"\n\n\nif __name__ == "__main__":\n    # Load the model from the XML file\n    model = mujoco.MjModel.from_xml_path(_XML_PATH.as_posix())\n\n    # Initialize the configuration for the model\n    configuration = mink.Configuration(model)\n\n    # Define the task for the end-effector\n    tasks = [\n        end_effector_task := mink.FrameTask(\n            frame_name="attachment_site",\n            frame_type="site",\n            position_cost=1.0,\n            orientation_cost=1.0,\n            lm_damping=1.0,\n        ),\n    ]\n\n    # Define collision pairs for collision avoidance\n    collision_pairs = [\n        (mink.get_body_geom_ids(model, model.body("wrist_3_link").id), ["floor", "wall"]),\n    ]\n\n    # Define limits for the configuration and collision avoidance\n    limits = [\n        mink.ConfigurationLimit(model=model),\n        mink.CollisionAvoidanceLimit(model=model, geom_pairs=collision_pairs),\n    ]\n\n    # Define maximum velocities for each joint\n    max_velocities = {\n        "shoulder_pan": np.pi,\n        "shoulder_lift": np.pi,\n        "elbow": np.pi,\n        "wrist_1": np.pi,\n        "wrist_2": np.pi,\n        "wrist_3": np.pi,\n    }\n    velocity_limit = mink.VelocityLimit(model, max_velocities)\n    limits.append(velocity_limit)\n\n    # Get the mocap ID for the target\n    target_mocap_id = model.body("target").mocapid[0]\n\n    # Initialize the model and data\n    model = configuration.model\n    data = configuration.data\n    solver = "quadprog"\n\n    # Launch the viewer for the model\n    with mujoco.viewer.launch_passive(\n        model=model, data=data, show_left_ui=False, show_right_ui=False\n    ) as viewer:\n        # Set the default free camera view\n        mujoco.mjv_defaultFreeCamera(model, viewer.cam)\n\n        # Initialize the configuration to the home keyframe\n        configuration.update_from_keyframe("home")\n        mujoco.mj_forward(model, data)\n\n        # Initialize the mocap target at the end-effector site\n        mink.move_mocap_to_frame(model, data, "target", "attachment_site", "site")\n\n        # Set up the rate limiter with a frequency of 500 Hz and suppress warnings\n        rate_limiter = RateLimiter(frequency=500.0, warn=False)\n        while viewer.is_running():\n            # Update the task target to the current position of the mocap target\n            target_pose = mink.SE3.from_mocap_name(model, data, "target")\n            end_effector_task.set_target(target_pose)\n\n            # Solve the inverse kinematics to compute the velocity\n            velocity = mink.solve_ik(\n                configuration, tasks, dt=rate_limiter.dt, solver=solver, damping=1e-3, limits=limits\n            )\n            # Integrate the computed velocity into the configuration\n            configuration.integrate_inplace(velocity, rate_limiter.dt)\n            # Update the model with the new configuration\n            mujoco.mj_forward(model, data)\n\n            # Optionally, update the camera light\n            mujoco.mj_camlight(model, data)\n\n            # Optionally, update the forward kinematics and sensor positions\n            mujoco.mj_fwdPosition(model, data)\n            mujoco.mj_sensorPos(model, data)\n\n            # Synchronize the viewer and sleep to maintain the desired frame rate\n            viewer.sync()\n            rate_limiter.sleep()\n