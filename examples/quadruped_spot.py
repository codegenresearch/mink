from pathlib import Path\n\nimport mujoco\nimport mujoco.viewer\nimport numpy as np\nfrom loop_rate_limiters import RateLimiter\n\nimport mink\n\n_HERE = Path(__file__).parent\n_XML = _HERE / "boston_dynamics_spot" / "scene.xml"\n\n\nif __name__ == "__main__":\n    # Load the model and data\n    model = mujoco.MjModel.from_xml_path(_XML.as_posix())\n    data = mujoco.MjData(model)\n\n    # Setup Inverse Kinematics (IK)\n    configuration = mink.Configuration(model)\n\n    feet_names = ["FL", "FR", "HR", "HL"]\n\n    # Define base task\n    base_task = mink.FrameTask(\n        frame_name="body",\n        frame_type="body",\n        position_cost=1.0,\n        orientation_cost=1.0,\n    )\n\n    # Define posture task\n    posture_task = mink.PostureTask(model, cost=1e-5)\n\n    # Define foot tasks\n    foot_tasks = []\n    for foot in feet_names:\n        task = mink.FrameTask(\n            frame_name=foot,\n            frame_type="geom",\n            position_cost=1.0,\n            orientation_cost=0.0,\n        )\n        foot_tasks.append(task)\n\n    # Define end-effector task\n    end_effector_task = mink.FrameTask(\n        frame_name="EE",\n        frame_type="site",\n        position_cost=1.0,\n        orientation_cost=1.0,\n    )\n\n    # Combine all tasks\n    tasks = [base_task, posture_task, *foot_tasks, end_effector_task]\n\n    # IK settings\n    base_target_id = model.body("body_target").mocapid[0]\n    foot_target_ids = [model.body(f"{foot}_target").mocapid[0] for foot in feet_names]\n    end_effector_target_id = model.body("EE_target").mocapid[0]\n\n    solver_type = "quadprog"\n    position_threshold = 1e-4\n    orientation_threshold = 1e-4\n    max_iterations = 20\n\n    # Launch the viewer\n    with mujoco.viewer.launch_passive(\n        model=model, data=data, show_left_ui=False, show_right_ui=False\n    ) as viewer:\n        mujoco.mjv_defaultFreeCamera(model, viewer.cam)\n\n        # Initialize the simulation\n        mujoco.mj_resetDataKeyframe(model, data, model.key("home").id)\n        configuration.update(data.qpos)\n        mujoco.mj_forward(model, data)\n\n        posture_task.set_target_from_configuration(configuration)\n        for foot, foot_target_id in zip(feet_names, foot_target_ids):\n            mink.move_mocap_to_frame(model, data, f"{foot}_target", foot, "geom")\n        mink.move_mocap_to_frame(model, data, "body_target", "body", "body")\n        mink.move_mocap_to_frame(model, data, "EE_target", "EE", "site")\n\n        rate_limiter = RateLimiter(frequency=500.0)\n        while viewer.is_running():\n            # Update task targets\n            base_task.set_target(mink.SE3.from_mocap_id(data, base_target_id))\n            for task, foot_target_id in zip(foot_tasks, foot_target_ids):\n                task.set_target(mink.SE3.from_mocap_id(data, foot_target_id))\n            end_effector_task.set_target(mink.SE3.from_mocap_id(data, end_effector_target_id))\n\n            # Compute velocity and integrate into the next configuration\n            for _ in range(max_iterations):\n                velocity = mink.solve_ik(configuration, tasks, rate_limiter.dt, solver_type, 1e-3)\n                configuration.integrate_inplace(velocity, rate_limiter.dt)\n\n                # Check if the position and orientation goals are achieved\n                position_achieved = True\n                orientation_achieved = True\n                for task in [end_effector_task, base_task, *foot_tasks]:\n                    error = task.compute_error(configuration)\n                    position_achieved &= bool(np.linalg.norm(error[:3]) <= position_threshold)\n                    orientation_achieved &= bool(np.linalg.norm(error[3:]) <= orientation_threshold)\n                if position_achieved and orientation_achieved:\n                    break\n\n            # Update control signals\n            data.ctrl = configuration.q[7:]\n            mujoco.mj_step(model, data)\n\n            # Visualize at fixed FPS\n            viewer.sync()\n            rate_limiter.sleep()\n